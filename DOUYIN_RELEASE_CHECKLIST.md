# 抖音小游戏上架与广告接入清单

## 当前项目已接入

- 抖音 AppID：`7647725019124451349`
- 激励视频广告位：`ad7664534731647715348`
- 插屏广告位：`ad7663666997092583444`
- 启动早期注册 `tt.onShow`，并通过 `getLaunchOptionsSync` 保留冷启动参数
- 使用 `tt.checkScene({ scene: 'sidebar' })` 检测侧边栏能力
- 使用 `tt.navigateToScene({ scene: 'sidebar' })` 引导用户进入侧边栏
- 从侧边栏返回游戏后发放每日一次的侧边栏体力奖励
- `tt.login({ force: false })` 静默检查登录态，不在启动时强制弹授权
- 激励视频仅由用户点击带视频标识的按钮触发，完整播放后才发奖
- 抖音激励视频全局复用一个实例
- 插屏启动后 30 秒内不展示，相邻插屏至少间隔 60 秒，激励视频后至少间隔 60 秒

## 发布前必须在开放平台确认

1. AppID 与当前抖音开放平台应用一致。
2. 两个广告位均属于当前 AppID，已启用且审核状态正常。
3. 基础信息、类目、图标、简介、玩法截图和版本说明已填写。
4. 隐私政策、用户信息处理规则与实际代码一致。本项目当前不需要用户输入文本，也未接入内购。
5. 如后续增加用户昵称、关卡名或其他自定义文本，必须补接敏感词检测。
6. 如后续增加虚拟支付，必须补接客服能力和平台支付规范要求。
7. 如请求网络资源，所有请求域名都已配置并使用 HTTPS/WSS。

## 真机验收

- 冷启动、热启动、切前后台和断网重连无报错。
- 首页在抖音环境仅显示受支持的侧边栏奖励入口，不显示国际 TikTok 桌面任务。
- 点击侧边栏入口可跳转；从抖音首页侧边栏再次进入后，奖励只发一次。
- 激励视频取消或中途关闭不发奖励；完整播放只发一次。
- 所有激励入口都有清晰的视频图标和奖励说明，不自动弹出广告。
- 启动 30 秒内没有插屏；激励视频后 60 秒内没有插屏；连续结算不会频繁弹插屏。
- 无广告填充、加载失败或弱网时，游戏仍可继续，且不会误发奖励。
- 中文和英文界面均无文字溢出、重叠或未翻译文案。

## 提审说明建议

可在审核备注中写明：

> 首页左上角为抖音侧边栏复访入口。点击后调用平台侧边栏跳转能力，从抖音首页侧边栏再次进入游戏可领取每日一次的 2 点体力。激励视频仅在玩家点击带视频图标的道具、复活或体力按钮后展示，完整播放后发放对应奖励。插屏仅在关卡完成的自然停顿点展示，并遵守平台频控。

